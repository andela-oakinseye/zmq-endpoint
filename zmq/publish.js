// import zeromq from 'zeromq';

// const pubber = zeromq.socket('pub');

const publish = (port, message) => {
  // const endpoint = `tcp://35.176.255.14:${port}`
  // console.log('Something before connect');
  
  // pubber.connect(endpoint);
  // console.log('Something to show that after connect');
  
  // pubber.send(message);
  console.log('Great')
};

export default publish;
